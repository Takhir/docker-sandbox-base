# Docker PHP, nginx, mysql/mariadb

Below are the minimal configuration files required to generate a Docker container with support for:

* PHP: 8.2
* Nginx
* Mariadb (MySQL)

To run commands use the following:

To build containers, run the command:
`docker-compose --build`

To run containers in the background (detached mode):
`docker-compose up -d`
